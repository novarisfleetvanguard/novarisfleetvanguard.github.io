# Logs
logs
*.log

node_modules

# Build output — regenerated by `bun run build` (the deploy consumes it from the
# CI artifact, never from git).
dist

# Local Workers runtime state (miniflare/wrangler), including per-room Durable
# Object storage from `bun run dev` and the test runner.
.wrangler
.dev.vars

.DS_Store
